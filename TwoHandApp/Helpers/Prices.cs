namespace TwoHandApp.Helpers;

public static class Prices
{
    public const decimal PremiumPrice = 5.00m;
    public const decimal VipPrice = 10.00m;
    public static readonly TimeSpan PremiumDuration = TimeSpan.FromDays(7);
    public static readonly TimeSpan VipDuration = TimeSpan.FromDays(7);
}